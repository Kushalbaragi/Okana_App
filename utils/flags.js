// Switches for features still being tried out. Flip one here and Fast Refresh
// picks it up — nothing to configure or redeploy.
export const FLAGS = {
  // BudgetSetupModal: set the monthly budget on a ruler instead of the keypad.
  // false brings the keypad back.
  budgetRuler: true,
};
